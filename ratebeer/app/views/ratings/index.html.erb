<h1>List of ratings</h1>
number of ratings <%= @ratings.count %>
<ul>
  <% @ratings.each do |rating| %>
  <li><%= rating %>, <%= link_to rating.user, user_path(rating.user) %></li>
  <% end %>
</ul>
<%= link_to "New rating", new_rating_path unless current_user.nil? %>